/**
 * ⚠️ uiStore — DEPRECATED re-export layer
 * New code should import from:
 *   - useModalStore  (modal state)
 *   - useMapUIStore  (basemap, tiles, opacity, low power)
 */
export { useModalStore } from './modalStore.ts'
export { useMapUIStore } from './mapUIStore.ts'
